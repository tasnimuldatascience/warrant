Results live in [`results/`](../../results/), not here.

One directory, because a reader looking for "what was measured" should not have to know
which of two conventions an author followed, and a link between two reports should not have
to guess a relative path. `docs/` carries the narrative — architecture, design decisions,
the resume point; `results/` carries the numbers those decisions were made from.
